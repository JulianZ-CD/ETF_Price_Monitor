# Production configuration - uses pre-built images only
# No local build required, fastest startup

services:
  # Nginx reverse proxy
  nginx:
    image: nginx:alpine
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - "80:80"
    depends_on:
      - frontend
      - backend
    restart: unless-stopped

  # Backend service - FastAPI application
  backend:
    image: ghcr.io/julianz-cd/etf_price_monitor-backend:latest
    expose:
      - "8000"
    volumes:
      - ./data:/app/data:ro
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://localhost:8000/api/py')",
        ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    restart: unless-stopped

  # Frontend service - Next.js application
  frontend:
    image: ghcr.io/julianz-cd/etf_price_monitor-frontend:latest
    expose:
      - "3000"
    environment:
      - DOCKER_ENV=true
      - DEPLOYMENT_MODE=multi
    depends_on:
      backend:
        condition: service_healthy
    restart: unless-stopped

